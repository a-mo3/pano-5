package com.ccscripts.panopticonfive.model.hiscore;

public enum HiscoreResponseType {
    SUCCESS,
    FAILURE,
    // if we can differentiate between rate limits and bans later we can add more
    ;
}
